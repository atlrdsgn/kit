{
  "name": "kit",
  "description": "an ever– expanding library of React components by @atlrdsgn",
  "private": true,
  "author": "chvndler",
  "license": "MIT",
  "packageManager": "yarn@3.6.3",
  "workspaces": [
    "apps/*",
    "pkgs/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "build:kit": "turbo run build --filter=kit",
    "build:apps": "turbo build --filter='./apps/*'",
    "build:pkg": "turbo build --filter='./pkgs/*'",
    "lint": "eslint . --ext .js,.jsx,.ts,.tsx",
    "clean": "turbo clean && rm -rf node_modules",
    "pretty": "prettier --write \"**/*.{ts,tsx,md}\"",
    "changeset": "changeset",
    "version-packages": "changeset version"
  },
  "resolutions": {
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "clsx": "2.0.0"
  },
  "devDependencies": {
    "@changesets/cli": "^2.26.2",
    "eslint": "^8.47.0",
    "eslint-config-atlrdsgn": "workspace:*",
    "prettier": "^3.0.2",
    "turbo": "^1.10.13"
  },
  "engines": {
    "node": "v18.16.1"
  }
}
