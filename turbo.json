{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.local", "NEXT_PUBLIC_SITE_URL"],
  "pipeline": {
    "build": {
      "outputs": ["dist/**", ".next/**", "!.next/cache/**"],
      "env": ["NEXT_PUBLIC_SITE_URL"],
      "dependsOn": ["^build"]
    },
    "lint": {
      "dependsOn": ["^build"],
      "inputs": [
        "**/*.{js,jsx,ts,tsx}",
        ".eslintignore",
        ".eslintrc.js",
        ".eslintrc"
      ]
    },
    "dev": {
      "dependsOn": ["^build"],
      "cache": false,
      "persistent": true
    },
    "clean": {
      "cache": false
    }
  }
}
